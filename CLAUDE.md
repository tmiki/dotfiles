# CLAUDE.md

このファイルはプロジェクト全体に適用されるコーディング規約を定義する。
実装仕様は `DOTFILES_SPEC.md` を参照すること。

---

## シェルスクリプトの構成規約

### ファイル構成

すべてのシェルスクリプトは以下のセクション順で構成すること。

```bash
#!/usr/bin/env bash
set -euo pipefail

# ==============
# 処理ステップ
# 1) ...
# 2) ...
# ==============

# ------------------------------------------------------------
# Globals
# ------------------------------------------------------------

# ------------------------------------------------------------
# Utils
# ------------------------------------------------------------

# ------------------------------------------------------------
# Functions
# ------------------------------------------------------------

# ------------------------------------------------------------
# Main
# ------------------------------------------------------------
main "$@"
```

### 各セクションの役割

**処理ステップコメント（ファイル冒頭）**
- スクリプトが何をどの順序で行うかを箇条書きで記載する
- 実装の前に読むだけで処理全体を把握できる粒度にすること

**Globals**
- スクリプト内で参照するグローバル変数を定義する
- `DOTFILES_DIR` のようなパス変数、定数はここに集約する
- 変数名はすべて大文字のスネークケース（例: `BACKUP_DIR`）

**Utils**
- スクリプト内で共通的に使う小さな処理を関数として定義する
- ログ出力関数（`info()` / `warn()` / `error()`）は必ずここに定義する
- ログ関数の実装:
  ```bash
  info()  { echo -e "\e[32m[INFO]\e[0m  $*"; }
  warn()  { echo -e "\e[33m[WARN]\e[0m  $*"; }
  error() { echo -e "\e[31m[ERROR]\e[0m $*"; }
  ```

**Functions**
- スクリプトの主要な処理を凝集性の高い単位で関数として定義する
- 1関数1責務を原則とする
- `main()` 関数はこのセクションの最後に定義する
- `main()` はステップを順に呼び出すだけの構成にし、処理の詳細は個別関数に委譲する
- 関数名はステップ番号のプレフィックスを付けず、処理内容を表す動詞+名詞で命名する（例: `install_ssh`, `backup_git`）

**Main（最終行）**
- `main "$@"` の1行のみ
- 関数定義より前に `main` を呼び出してはならない

### コメント規約

- セクション区切りは `# ---` の罫線コメントを使用する
- 処理の意図が自明でない箇所には日本語コメントを付与する
- 引数・戻り値の説明が必要な関数には関数直前にコメントを記載する

### その他

- `DOTFILES_DIR` は必ずスクリプトの絶対パスから算出し、相対パス依存を排除する:
  ```bash
  DOTFILES_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
  ```
- Linux (Ubuntu) / Windows (Git Bash) の両環境で動作すること
- コピー処理には `cp` を使用し、シンボリックリンクは使用しない
